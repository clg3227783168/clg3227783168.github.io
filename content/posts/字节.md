# 从客户端发出一个HTTP请求，一直到请求结束。整个过程当发生了什么
1. 客户端解析URL，提取协议、域名、端口、路径
2. 检查DNS缓存：如果存在，直接获取到目标IP地址，执行4；如果不存在，执行3
3. 发送DNS查询请求经由路由器到DNS服务器，查询目标IP地址
4. 客户端根据目标IP地址和服务端通过三次握手建立TCP连接
5. 

## 三次握手

### 第一次
发送SYN报文（Synchronize Sequence Numbers），请求建立连接，


# HTTPS跟HTTP的区别 除了这个安全性上面其他方面有什么区别吗？比如它的访问速度有区别吗？然后它的传输，呃，传输的内容有区别吗？

# 栈和队列的核心区核心特性是什么？然后他们有什么典型的应用场景

# kafka消息队列

# 解C++当中的内存泄露？概念是什么，有哪些场景。

# 指针跟引用它的核心区别是什么

# C++ 多态的实现原理

